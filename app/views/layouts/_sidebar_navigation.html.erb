<nav id="sidebar">
  <div id="sidebar-banner" role="banner" class="bg-gray-700 mb-12 flex items-center">
    <div class="left-buttons flex-1"></div>

    <div class="right-buttons flex-1 flex items-center justify-end">
      <button class="py-2 px-5 rounded text-gray-500 hover:text-gray-300 visible md:invisible"
              type="button"
              data-sidebar-target="toggle"
              data-action="sidebar#toggleSidebar"
              aria-controls="sidebar"
              aria-expanded="false"
              aria-label="Toggle navigation">
        <%= fa_icon 'times', icon_class: 'fa-lg' %>
      </button>
    </div>
  </div>

  <section>
    <%= sidebar_link_to 'Dashboard', dashboard_path, icon: 'tachometer-alt' %>
  </section>

  <section class="mt-8">
    <div class="uppercase text-sm tracking-wide text-gray-600 px-4">Admin</div>
    <%= sidebar_link_to 'Manage Users', '#', icon: 'users-cog' %>
  </section>

  <section class="mt-8">
    <div class="uppercase text-sm tracking-wide text-gray-600 px-4">Account</div>
    <%= sidebar_link_to 'Reset Password', new_user_password_path, icon: 'unlock-alt' %>
    <%= sidebar_link_to 'Sign out', destroy_user_session_path, method: :delete, icon: 'sign-out-alt' %>
  </section>
</nav>
